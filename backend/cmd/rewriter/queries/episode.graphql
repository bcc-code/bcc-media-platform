query getEpisode($id: ID!) {
    episode(id: $id) {
        title
        index
        image
        number
        season {
            title
            show {
                title
            }
        }
    }
}
