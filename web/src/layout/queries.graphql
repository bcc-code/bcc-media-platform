query getCalendarStatus($day: Date!) {
    calendar {
        day(day: $day) {
            entries {
                start
                end
            }
        }
    }
}

query application {
    application {
        code
        page {
            code
        }
        searchPage {
            code
        }
    }
}
