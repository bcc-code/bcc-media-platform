query getPlaylistEpisode($id: ID!) {
    playlist(id: $id) {
        items(first: 10) {
            items {
                __typename
                ... on Episode {
                    id
                }
            }
        }
    }
}
