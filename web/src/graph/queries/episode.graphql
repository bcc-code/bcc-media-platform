query getEpisode($episodeId: ID!) {
    episode(id: $episodeId) {
        id
        title
        description
        imageUrl
        number
        season {
            title
            number
            show {
                title
            }
        }
    }
}
