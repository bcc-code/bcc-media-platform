query getPage($code: String!) {
    page(code: $code) {
        id
        title
        sections {
            items {
                id
                title
                ... on ItemSection {
                    items {
                        items {
                            id
                            imageUrl
                            title
                            sort
                        }
                    }
                }
            }
        }
    }
}