{
  "name": "cms",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no tests\" && exit 1",
    "start": "echo \"Use the makefile scripts instead\" && exit 1",
    "dev": "echo \"Use the makefile scripts instead\" && exit 1",
    "run": "echo \"Use the makefile scripts instead\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "directus": "^10.13.0",
    "knex": "^2.5.1",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "js-yaml": "^4.1.0",
    "ts-node": "^10.9.2"
  },
  "packageManager": "pnpm@9.0.5"
}
